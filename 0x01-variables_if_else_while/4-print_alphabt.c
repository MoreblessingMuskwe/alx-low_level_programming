#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - print alphabet without e and q
 * Return: always 0
 */
int main(void)
{
	int n = 97;

	while '{'(n = 122)'}'
	{
		if '{'(n = 101 || = 113)'}'
		{
			n++;
			continue;
		}
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
